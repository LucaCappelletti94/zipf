from multiprocessing import

class jsd:
    def __init__(self, path, zipfs, output, use_cli = False):
        self._path = path
        self._zipfs = zipfs
        self._output = output
        self._use_cli = use_cli

    def job(self):

    def run(self):
